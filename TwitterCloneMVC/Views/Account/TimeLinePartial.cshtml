@model IEnumerable<TwitterCloneMVC.Models.Tweet>
<div class="row">
    <div class="container">
        <h4>Time Line</h4>
        <div class="row">

            <div class="col-md-12 col-lg-12">
                <div id="tracking-pre"></div>
                <div id="tracking">
                    <div class="text-center tracking-status-intransit">
                        <p class="tracking-status text-tight">in transit</p>
                    </div>
                    <div class="tracking-list">
                        @foreach (var item in Model)
                        {
                            <div class="tracking-item">
                                <div class="tracking-icon status-intransit">
                                    <svg class="svg-inline--fa fa-circle fa-w-16" aria-hidden="true" data-prefix="fas" data-icon="circle" role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" data-fa-i2svg="">
                                        <path fill="currentColor" d="M256 8C119 8 8 119 8 256s111 248 248 248 248-111 248-248S393 8 256 8z"></path>
                                    </svg>
                                    <!-- <i class="fas fa-circle"></i> -->
                                </div>
                                <div class="tracking-date">@item.Created.Value.Date.ToString("yyyy-MM-dd")<span>@item.Created.Value.TimeOfDay</span></div>
                                <div class="tracking-content">@item.user_id<span>@item.message</span></div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
